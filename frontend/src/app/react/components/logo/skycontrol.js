import React from 'react';
import { colors, Text } from 'react-elemental';

const Skycontrol = () => (
  <div>
    <Text size="delta" color={colors.gray5} inline uppercase>sky</Text>
    <Text size="delta" color={colors.primary} inline uppercase>control</Text>
  </div>
);

export default Skycontrol;
